---
title: "Методы условной оптимизации"
author: Семинар
institute: ФКН ВШЭ
format:
    beamer:
        pdf-engine: xelatex
        aspectratio: 169
        fontsize: 9pt
        section-titles: false
        incremental: true
        include-in-header: ../files/xeheader.tex
---

# Повторение лекции. Проекция
## Проекция

**Расстояние** $d$ от точки $\mathbf{y} \in \mathbb{R}^n$ до замкнутого множества $S \subset \mathbb{R}^n$:
$$
d(\mathbf{y}, S, \| \cdot \|) = \inf\{\|x - y\| \mid x \in S \}
$$
Рассмотрим **евклидову проекцию** (возможны и другие варианты) точки $\mathbf{y} \in \mathbb{R}^n$ на множество $S \subseteq \mathbb{R}^n$ — это точка $\text{proj}_S(\mathbf{y}) \in S$:
$$
\text{proj}_S(\mathbf{y}) = \frac12 \underset{\mathbf{x} \in S}{\operatorname{argmin}} \|x - y\|_2^2
$$

* **Достаточные условия существования проекции**. Если $S \subseteq \mathbb{R}^n$ — замкнутое множество, то проекция на множество $S$ существует для любой точки.
* **Достаточные условия единственности проекции**. Если $S \subseteq \mathbb{R}^n$ — замкнутое выпуклое множество, то проекция на множество $S$ единственна для любой точки.
* Если множество открытое, а точка лежит вне него, то её проекция на это множество не существует.
* Если точка принадлежит множеству, то её проекция — она сама.

## Проекция

:::: {.columns}

::: {.column width="65%"}

:::{.callout-tip title="Теорема о неравенстве Бурбаки–Чейни–Голдштейна"}
\small
Пусть $S \subseteq \mathbb{R}^n$ — замкнутое выпуклое множество, $\forall x \in S, y \in \mathbb{R}^n$. Тогда
$$
\langle y - \text{proj}_S(y), \mathbf{x} - \text{proj}_S(y)\rangle \leq 0
$$ {#eq-proj1}
$$
\|x - \text{proj}_S(y)\|^2 + \|y - \text{proj}_S(y)\|^2 \leq \|x-y\|^2
$$ {#eq-proj2}
:::

::: {.callout-tip title="Нерастягивающая функция"}
Функция $f$ называется **нерастягивающей**, если $f$ является $L$-Липшицевой при $L \leq 1$ ^[Нерастягивающая функция становится сжимающей, если $L < 1$.]. То есть для любых двух точек $x,y \in \text{dom} f$,
    $$
    \|f(x)-f(y)\| \leq L\|x-y\|, \text{ где } L \leq 1.
    $$
    Это означает, что расстояние между образами точек не превышает расстояния между самими точками.
:::

:::

::: {.column width="35%"}
![Угол для любой точки $x \in S$ должен быть тупым или прямым](proj_crit.pdf)
:::

::::

## Задачи

::: {.callout-question}
Является ли оператор проекции нерастягивающим?
:::

::: {.callout-question}
Найдите проекцию $\text{proj}_S(\mathbf{y})$ на $S$, где $S$:

* $l_2$-шар с центром 0 и радиусом 1:
$$
S = \{x\in\mathbb{R}^d \vert \;\Vert x \Vert^2_2=\sum_{i=1}^d x_i^2 \leq 1\}
$$
* Куб в $\mathbb{R}^d$:
$$
S = \{x\in\mathbb{R}^d \vert \;a_i\leq x_i \leq b_i\}
$$
* Аффинные ограничения:
$$
S = \{x\in\mathbb{R}^d \vert \;Ax=b\}
$$
:::
# Задачи

## Задача 1
Пусть $\mathcal{L}_L$ обозначает множество матриц, являющихся $L$-Липшицевыми:
    $$
    \mathcal{L}_L = \left\{ A \in \mathbb{R}^{m \times n} \,\middle|\, \|Ax - Ay\|_2 \leq L\|x - y\|_2 \;\; \forall x, y \in \mathbb{R}^n \right\}.
    $$

\textbf{Задача проекции:}
    Дана матрица $M \in \mathbb{R}^{m \times n}$. Найти матрицу $X \in \mathcal{L}_L$, ближайшую к $M$ в некоторой норме:
    $$\mathcal{P}_{(L,\zeta)} : \quad \underset{X \in \mathcal{L}_L}{\mathrm{argmin}} \; \frac{1}{2} \|X - M\|_\zeta^2.$$

На самом деле, при $L = 1$ и $\zeta = F$ задача имеет простое решение: можете ли вы предложить идею решения в этом случае?

## Задача 2

Пусть $\mathcal{S}$ — неотрицательный ортант. Найдите следующую проекцию:
    $$\text{proj}_{\mathcal{S}}(\mathbf{y}) = \underset{\mathbf{x} \geq 0}{\arg\min} \|\mathbf{x} - \mathbf{y}\|_2$$

где $\mathbf{x} \geq 0$ означает, что $\mathbf{x}$ лежит в неотрицательном ортанте $\mathcal{S} = \left\{ \mathbf{x} \mid x_i \geq 0 \; \forall i \right\}$.

Что изменится, если $\mathcal{S} = \{x | l \leq X \leq u\}$?

## Задача 3

Спектраплекс — это спектраэдр, определяемый как множество
$$
\mathcal{S} := \left\{ X \in \mathbb{S}_+^n : \operatorname{Tr} X = 1 \right\},
$$

Спектраплекс = «спектра» + «симплекс», то есть «собственные значения в симплексе».
Спектраплекс — «полуопределённый» аналог симплекса.

Вопрос: дана матрица $Z \in \mathbb{R}^{n \times n}$. Что такое проекция $Z$ на множество $\mathcal{S}$?

Другими словами, решите следующую задачу оптимизации:


$$\underset{X \succeq 0,\, \operatorname{Tr} X = 1}{\arg\min} \; \frac{1}{2} \| X - Z \|_F^2.$$


# Повторение лекции. Метод проекции градиента
## Метод проекции градиента. Идея

$$
x_{k+1} = \text{proj}_S\left(x_k - \alpha_k \nabla f(x_k) \right)  \qquad \Leftrightarrow \qquad \begin{aligned}
y_k &= x_k - \alpha_k \nabla f(x_k) \\
x_{k+1} &= \text{proj}_S\left( y_k\right)
\end{aligned}
$$

Ниже приведён пример применения этого метода для атаки нейронной сети:
[\faPython Атаки состязательными примерами](https://colab.research.google.com/drive/1Azj1N0CN2WYm5x-IZsQyz5p77CRdKt9d?usp=sharing).


![Иллюстрация алгоритма метода проекции градиента](PGD.pdf)


# Повторение лекции. Метод Франка-Вульфа
## Метод Франка-Вульфа. Идея

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW1.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW2.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW3.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW4.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW5.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW6.pdf)

## Метод Франка-Вульфа. Идея {.noframenumbering}

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW7.pdf)

## Метод Франка-Вульфа. Идея

$$
\begin{split}
y_k &= \text{arg}\min_{x \in S} f^I_{x_k}(x) = \text{arg}\min_{x \in S} \langle\nabla f(x_k), x \rangle \\
x_{k+1} &= \gamma_k x_k + (1-\gamma_k)y_k
\end{split}
$$

![Иллюстрация алгоритма Франка-Вульфа (условного градиента)](FW.pdf)

# Скорость сходимости
## Скорость сходимости для гладкого выпуклого случая

:::{.callout-theorem}
Пусть $f: \mathbb{R}^n \to \mathbb{R}$ — выпуклая и дифференцируемая функция. Пусть $S \subseteq  \mathbb{R}^n$ — замкнутое выпуклое множество, и предположим, что существует минимизатор $x^*$ функции $f$ на $S$; кроме того, предположим, что $f$ гладкая на $S$ с параметром $L$.

* Алгоритм **метода проекции градиента** с шагом $\frac1L$ обеспечивает следующую скорость сходимости после итерации $k > 0$:
$$
f(x_k) - f^* \leq \frac{L\|x_0 - x^*\|_2^2}{2k}
$$

* **Метод Франка-Вульфа** обеспечивает следующую скорость сходимости после итерации $k > 0$:
$$
f(x_k) - f^* \leq \frac{2L\|x_0 - x^*\|_2^2}{k+1}
$$

:::

:::{.callout-tip title="Особенности метода Франка-Вульфа"}

* Скорость сходимости метода Франка-Вульфа для $\mu$-сильно выпуклых функций равна $\mathcal{O} \left( \dfrac{1}{k} \right)$

* Метод Франка-Вульфа не работает для негладких функций. Однако его модификации работают.

* Метод Франка-Вульфа работает для любой нормы.

:::

# Зеркальный спуск
## Метод субградиента: линейное приближение + штраф за близость

Вспомним шаг метода субградиента с субградиентом $g_k$:
$$
x_{k+1} = x_k - \alpha_k g_k  \qquad \Leftrightarrow \qquad \begin{aligned}
x_{k+1} &= \underset{x}{\operatorname{argmin}} \; \underbrace{f(x_k) + g_k^\top(x-x_k)}_{\text{линейное приближение } f}+\underbrace{\dfrac{1}{2\alpha} \|x - x_k\|_2^2}_{\text{штраф за близость}} \\
 &= \underset{x}{\operatorname{argmin}} \; \alpha g_k^\top x+\dfrac{1}{2} \|x - x_k\|_2^2
\end{aligned}
$$

![$\Vert\cdot\Vert_1$ не является сферически симметричной](EuclideanGeometry.pdf){width=160}

## Пример. Плохая обусловленность

Рассмотрим $f(x_1, x_2)=x_1^2\cdot\dfrac{1}{100}+x_2^2\cdot 100$.

![Плохо обусловленная задача в норме $\Vert\cdot\Vert_2$](BadCondition.pdf)

## Пример. Плохая обусловленность
Предположим, мы находимся в точке: $x_k=(-10 \quad -0.1)^\top$.
Метод субградиента: $x_{k+1} = x_k - \alpha\nabla f(x_k)$
$$
\nabla f(x_k) = (\dfrac{2x_1}{100} \quad 2x_2\cdot 100)^\top\Big\vert_{(-10 \; -0.1)^\top}=\left(-\dfrac{1}{5} \quad -20\right)^\top
$$

**Проблема:** из-за вытянутости линий уровня направление движения $(x_{k+1}-x_k)$ приблизительно $\perp$ $(x^*-x_k)$.

**Решение:** заменим штраф за близость
$$
x_{k+1} = \underset{x}{\operatorname{argmin}} \; \underbrace{f(x_k) + g_k^\top(x-x_k)}_{\text{линейное приближение } f}+\underbrace{\dfrac{1}{2\alpha} (x - x_k)^\top \textcolor{orange}{I} (x-x_k)}_{\text{штраф за близость}}
$$
на другой
$$
x_{k+1} = \underset{x}{\operatorname{argmin}} \; \underbrace{f(x_k) + g_k^\top(x-x_k)}_{\text{линейное приближение } f}+\underbrace{\dfrac{1}{2\alpha} (x - x_k)^\top \textcolor{orange}{Q} (x-x_k)}_{\text{штраф за близость}},
$$
где $Q=\begin{pmatrix}
    \frac{1}{50} & 0\\
    0 & 200
\end{pmatrix}$ для данного примера. А в более общем случае — на другую функцию $\color{orange}{B_\phi(x, y)}$, измеряющую близость.

## Пример. Плохая обусловленность
Найдём $x_{k+1}$ для этого **нового** алгоритма
$$
\alpha\nabla f(x_k) + \begin{pmatrix}
    \frac{1}{50} & 0\\
    0 & 200
\end{pmatrix}(x-x_k) = 0.
$$
Решая относительно $x$, получаем
$$
x_{k+1} = x_k - \alpha \begin{pmatrix}
    50 & 0\\
    0 & \frac{1}{200}
\end{pmatrix} \nabla f(x_k) = (-10 \; -0.1)^\top - \alpha (-10 \; -0.1)^\top
$$
**Наблюдение:** меняя штраф за близость, мы **меняем направление** $x_{k+1}-x_k$. Иными словами, если мы измеряем расстояние таким **новым** способом, то также **меняем константу Липшица**.

::: {.callout-question}
Какова константа Липшица функции $f$ в точке $(1 \; 1)^\top$ для нормы:
$$
\Vert z \Vert^2 = z^\top \begin{pmatrix}
    50 & 0\\
    0 & \frac{1}{200}
\end{pmatrix} z?
$$
:::

## Пример. Устойчивая регрессия

Квадратичная функция потерь $\Vert Ax-b\Vert^2_2$ очень чувствительна к выбросам.

**Альтернатива:** $\min \Vert Ax-b\Vert_1$. Эта задача также **выпуклая**.

Вычислим константу Липшица $L$ для $f(x)=\Vert Ax-b\Vert_1$:
$$
\vert \Vert Ax-b\Vert_1 - \Vert Ay-b\Vert_1 \vert \leq L \Vert x-y \Vert_2.
$$
Для упрощения вычислений: $A=I$, $b=0$, т.е. $f(x)=\Vert x \Vert_1$.

Возьмём $x=\bold{1}_d$, $y=(1+\varepsilon)\bold{1}_d$:
$$
\vert n - (1+\varepsilon)n\vert = \varepsilon n \leq L \Vert x - y \Vert_2=\Vert -\varepsilon \Vert_2=\sqrt(n\varepsilon^2)=\varepsilon\sqrt{n}.
$$
Итого получаем $\color{orange}{L=\sqrt{n}}$. Как видим, $L$ **зависит от размерности**.


::: {.callout-question}
Покажите, что если $\Vert\nabla f(x)\Vert_\infty \leq 1$, то $\Vert\nabla f(x)\Vert_2 \leq \sqrt{d}$.
:::

# Список литературы
## Список литературы

Примеры для раздела о зеркальном спуске взяты из лекции [\faYoutube](https://www.youtube.com/watch?v=m_SJafYedbQ&list=PLXsmhnDvpjORzPelSDs0LSDrfJcqyLlZc&index=21).
